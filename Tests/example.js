let x = 5; //x is a number
x = "Good morning"; //x is now a string (no error)
console.log(x); // Output: Good morning

const rectangleArea = { width: 30, height: 40 };
const Area = rectangleArea.width * rectangleArea.something;
console.log(Area);
